"""financial_data.py — placeholder.

The financial-data tool is now implemented as the analyze subgraph in
src/graph/compute_subgraph/, exposed via the `analyze_user_finances` tool
declared in src/graph/nodes.py.
"""
